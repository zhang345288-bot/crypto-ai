# Example environment variables for backend
# Copy this file to .env and fill values before running backend locally

# Google Gemini API key (optional, only for AI analysis)
GEMINI_API_KEY=AIza_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# CORS allowed origins (comma-separated) e.g. https://localhost:3000
ALLOWED_ORIGINS=http://localhost:3000

# Other optional config
ENV=development
# ============================================================================
# Gemini API Key - 用於 AI 深度分析（可選）
# ============================================================================
# 從 https://ai.google.dev/ 獲取免費的 API key
# 若不設置此項，技術分析仍可正常使用，只是沒有 AI 深度分析功能
# 免費版每日限制：約 1500 請求/天 或 100 萬 tokens/天
GEMINI_API_KEY=your-gemini-api-key-here

# ============================================================================
# CORS 設置 - 允許的域名
# ============================================================================
# 可允許的域名（開發時常用 localhost）
ALLOWED_ORIGINS=http://localhost:3000

# ============================================================================
# 環境設置
# ============================================================================
ENV=production
PORT=8000
